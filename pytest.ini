[pytest]
addopts = -m "not network"
markers =
    performance: 延遲預算測試（spec 004 US2）；本機快速迭代可用 -m "not performance" 排除，CI 預設全跑
    network: 需外部網路之測試（spec 010）；預設排除（addopts）——本機驗收以 pytest -m network 顯式執行，CI 永不出網
